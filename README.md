entityreference_lazyreference
=============================

This is an extension to the entityreference drupal module which allows us to create lazy reference: create entities and references after creating the referencing entity.